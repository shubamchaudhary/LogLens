# HANDOVER — ChunkAI modernization (multi-session)

> **Protocol for every agent/session:** read this file FIRST, then the two architecture docs. Do the work. Before ending the session, update **Current state**, **Next steps**, and append one row to the **Session log**. Keep this file short — it's a pointer board, not a diary. Details belong in the numbered docs.

## Project intent (stable — don't re-litigate)

Owner (Shubam) is building this for **interview-portfolio strength**; every technology must be justifiable problem-first (justifications live in the docs).

**Finalized positioning (2026-07-14): log intelligence pipeline.** Analyze production log archives at ingest time — deterministic parsers for exact metrics, LLM enrichment (via Kafka partition-per-API-key lanes) for anomaly explanations and incident correlation — materialize into structured tables, generate a report, answer fixed-param user queries from those tables via SQL, with a grounded RAG drill-down to raw log lines. "Ingest-time RAG." Document Q&A (v1 feature set) stays as a secondary mode. This supersedes the earlier "generic RAG upgrade" direction.

## Key docs

| File | Contents |
|---|---|
| [01-current-architecture.md](01-current-architecture.md) | Verified deep-dive of the v1 system: schema, data flows, key-slot worker pool, recursive summarization, limitations (§9), interview Q&A |
| [02-target-architecture.md](02-target-architecture.md) | **FINAL target design**: log-intelligence positioning, whiteboard diagram (§3), cut list (§4), two-layer extraction + taxonomy, data model + ER + file lifecycle (§6), Kafka lanes, LangGraph graphs, query model, interview Q&A (§12) |
| [03-implementation-spec.md](03-implementation-spec.md) | **Code-level spec, agent-executable**: global binding decisions, message schemas, topics, Phases 0–5 each sized for one PR with new-files/deletions/definition-of-done. Execute in order. |
| [/schema-v2.sql](../schema-v2.sql) | The v2 table-creation script (fresh DB; replaces init.sql in compose) incl. per-session chunk-table template |

## Locked design decisions (with their defense in 02)

1. **Two-layer extraction** — parsers for numbers, LLMs for meaning; enrich anomalous windows only (02 §5).
2. **Kafka partition-per-API-key** for LLM lanes; at-least-once + idempotent handlers (fingerprint dedup, natural-key upserts); retry topic for 429s; DLQ. Four topics total (02 §7).
3. **Table-per-session for raw chunks** (`log_chunks_s_{sessionId}`, own HNSW **+ GIN full-text**) — justified by corpus-scale sessions (10⁵–10⁶ chunks); ceiling ~tens-of-thousands of tables; exit = hash partitioning; names from validated UUIDs only (02 §6).
4. **Query time = SQL over materialized tables**; drill-down RAG (Graph 2, session-scoped pgvector + GIN) is the only query-time LLM path (02 §9).
5. **CUT LIST (refinement pass, do not re-add without cause):** Elasticsearch, RRF/hybrid fusion, durable object storage (see #8), query-time recursive summarization (legacy doc-QA mode only), async query API + status topic (SSE reads the DB status row), dedicated vector DB, Kafka exactly-once. Reasons in 02 §4 — that section is interview material.
6. **Chunk logs by time window**, not by page/size.
7. **$0 constraint**: whole stack free/self-hosted; Gemini free tier.
8. **Staging blob lifecycle (owner-specified):** uploads land in **MinIO** (self-hosted, S3-compatible, free — one docker-compose service, used from local dev through prod, no separate "local disk" code path); chunker *streams* the file (never whole-in-memory); after processing the staged file is **deleted** (`documents.staged_file_deleted`) — the chunks in Postgres are the durable copy (02 §6).
9. **Core entity tables (owner-specified):** `users` (user_id PK, email, password_hash, full_name, created_at, last_login_at, is_active — matches v1 init.sql), `sessions` (session_id PK, user_id FK, status+counters), `documents` (document_id PK, user_id FK, session_id FK, file_url, status). ER diagram in 02 §6.

## Facts that save you an hour of re-reading

- Backend = ONE Gradle module `deepdocai-backend`, packages `api/common/core/data/llm`.
- Key concurrency today is **thread-pinned key slots** (NOT round-robin) — 5 keys, 7.5s/key, 429 → re-enqueue + 60s sleep. Maps 1:1 to Kafka partition-per-key; that's the central migration insight.
- Job queue was `processing_jobs` + 3s polling + lock leases (`ProcessingJobWorker`) — **DELETED in Phase 1** (lives in git history only).
- DB on port **5434** via docker-compose; schema in `init.sql` (no Flyway yet).
- Transactions deliberately split around long LLM calls (short TX → no TX → short TX) — preserve in refactors.
- Existing prompts already log-analysis flavored (`DocumentAnalysisPrompts` mentions stack traces, thread states, pool events) — the pivot matches the code's direction.
- Vector search today: `ORDER BY embedding <=> :q LIMIT 500` two-query pattern in `DocumentChunkRepositoryImpl`; known issues: could be single query; no `ef_search` tuning; `LIMIT 500` > default `ef_search=40` is incoherent. Superseded by target design's per-session tables.
- Frontend `examprep-frontend` still has old naming; low priority. Git branch `main`; write real commit messages (history is full of "m").

## Current state (update each session)

**2026-07-15 (frontend v2 shell resumed):** the phase-5 backend is already in place, and the current repo work is the v2 React shell. Fixed the broken API client in `examprep-frontend/src/services/api.js` (Bearer token header + SSE fetch path) and verified `npm run build` succeeds. The new session sidebar/workspace/progress/report/findings/incidents/metrics/drilldown components are wired against the phase-5 endpoints; next work is live `npm run dev` QA and any remaining frontend polish.

**2026-07-15 (Phases 4 & 5 DONE — full pipeline verified end-to-end; query APIs + SSE live).** Branch `phase-5-query-sse` (off `phase-4-orchestrator`), local-only.

**Phase 4 VERIFIED end-to-end** on the Groq-chat + Gemini-embed split: a real pipeline run produced **16 findings → 6 incidents → ~1.4 KB report**, **5/5 chunks embedded** (`gemini-embedding-001`@768 — no longer NULL), and **drilldown returns cited answers** (both graphs green). Orchestrator fixes (my `8b2950b`; recursion fix folded into owner's `4a1535a`): (a) dynamic `recursion_limit = 12 + n_findings*(MAX_CORRELATE_ATTEMPTS*2+1)` in `graph_analyze.run_analysis` — llama's ground_check rejects more than Gemini did, so Graph 1's per-cluster regenerate loop blew LangGraph's default 25-step budget → FAILED; the loop was already bounded (force-accept at last attempt), it just needed headroom; (b) `graph_drilldown.generate_node` falls back **graded→docs→best_docs** so an over-strict grader / degrading rewrite can't answer from nothing; (c) `db.count_findings` helper.

**Phase 5 DONE — new `com.deepdocai.analysis` package:** `AnalysisQueryController` (`GET /api/v1/sessions/{id}/{metrics,findings,incidents,report,evidence}` + `POST /{id}/drilldown`), `AnalysisReadRepository` (JDBC reads; evidence via validated-UUID chunk-table name), `AnalysisProgressController` (`GET /{id}/progress` SseEmitter; one `@Scheduled(fixedDelay=1500)` sweep pushes `{status,enriched,total}` to all open emitters + initial snapshot; completes on DONE/FAILED). Every endpoint gated by `SessionAccess` → **404 (missing) / 403 (not owner)**; the guard is **returned not thrown** (the KEEP `GlobalExceptionHandler` catch-all `Exception`→500 would otherwise clobber a 403/404 — same reason `SessionController` returns explicit statuses). Also: `OrchestratorClient` **converted RestClient→WebClient** (the hand-built `RestClient` sent an empty body → FastAPI **422**; WebClient with `.bodyValue(...)` is the codebase's proven JSON-POST client, matching `GroqClient`) + 120 s drilldown timeout; `@EnableScheduling` on the app class; `SecurityConfig` permits `DispatcherType.ASYNC/ERROR` (removes the SSE-completion `AccessDenied`-on-committed-response stack traces). CORS already allows `:5173`/`:3000` — no change needed for the Vite frontend.

**DoD verified via curl (fresh user, real pipeline run, session `35aabc00`):** create session → upload sample → **SSE streamed ENRICHING 1/3→2/3→CORRELATING→REPORTING→DONE** (live sweep confirmed by flipping the DB status mid-stream) → `metrics`(44) / `findings`(16, severity-ordered) / `incidents`(6) / `report`(1404 ch) / `evidence`(chunk-table read) → `drilldown` grounded answer + citation via the Java→orchestrator proxy. Gates: other-user **403**, missing session **404**, no-token **403**. ⚠️ **NB:** orchestrator Groq calls are NOT lane-rate-limited (only the Java enrichment side is) — a very large session could 429 mid-graph; sample sizes (≤~20 findings) run clean. Enrichment throughput is Groq-TPM-bound (~6K TPM) so a 3-window sample can sit ~50 s at "ENRICHING n/total" while the retry lane drains 429s — expected, not a hang.

---

**2026-07-15 (verified from owner's AI Studio limits):** Gemini Embedding 1/2 free tier = **100 RPM / 30K TPM / 1,000 RPD each** → batched at 80, ~80K chunks/day on the one default model (`gemini-embedding-001`) — current setup is quota-sufficient; the earlier "check embedding limits" warning is resolved. **Future scaling lever (designed, deliberately NOT built — nothing is quota-starved):** generalize lanes to (provider, model, key) to pool per-model chat quotas (Groq 8B 14.4K + Gemma 4 26B/31B 14.4K each + Flash Lite 500 RPD); constraint: embedding lanes need **session affinity** (one embedding model per session, stored on the session row) because vector spaces from different models never mix — chat lanes have no such constraint.

**2026-07-15 (CORRECTION `0279276`: embedding provider SPLIT from chat — Groq serves NO embedding models).** Verified against console.groq.com/docs/models + owner's org-limits screenshot: Groq is chat+Whisper only; `nomic-embed-text-v1_5` was misinformation from unofficial docs, and EMBED_BATCH on Groq would 404. Now: **chat=groq, embeddings=gemini** (`chunkai.llm.embedding-provider` / `EMBEDDING_PROVIDER`, default gemini; Java gateway passes null lane-key so GeminiClient resolves its own key; Python `embed_query` uses `embed_api_key()`). Both sides MUST agree. Committed the previously-uncommitted `outputDimensionality` fix in GeminiClient/GeminiConfig — load-bearing (gemini-embedding-001 is 3072-dim without it, breaking vector(768)). **Both keys now required in .env**: GROQ_API_KEY (chat) + GEMINI_API_KEY (embeddings). Also noted: Groq 8b-instant's real throttle is **6K TPM**, not 30 RPM → expect 429→retry-lane churn on fat windows (functional by design). Leftover uncommitted: `graph_drilldown.py` best_docs WIP (not mine, left for owner).

---

_Superseded by the correction above (kept for detail):_ **2026-07-15 (Groq migration DONE — Groq is now the DEFAULT LLM provider).** Branch `phase-4-orchestrator`, local-only. Reason: Gemini free tier collapsed to 5 RPM / **20 RPD** per project (owner's AI Studio screenshot) — unusable for enrichment. Groq free tier: 30 RPM / 14,400 RPD on `llama-3.1-8b-instant`, plus embeddings via `nomic-embed-text-v1_5` (**natively 768-dim → drop-in for vector(768), no schema change**).

- **New:** `llm/client/GroqConfig` (`groq.*` props: base-url `https://api.groq.com/openai/v1`, chat `llama-3.1-8b-instant`, embed `nomic-embed-text-v1_5`, 30 RPM), `GroqClient` (OpenAI-compatible chat + batch embeddings; same contract: 429→`RateLimitException`, 5xx/connect retry in-place, other 4xx fail fast; embeddings re-ordered by response `index`), `LlmGateway` (provider switch on `chunkai.llm.provider`, default **groq**; also owns `toVectorString`).
- **Modified:** `EnrichConsumer` → talks to `LlmGateway` (only GeminiClient call site); partition→lane now `partition % slotCount` (guards stale topics wider than the new key count — Kafka can't lower partitions; recreate topic/volume for clean 1:1). `ApiKeyManagerConfig` → builds lanes from the ACTIVE provider's keys + rate (groq 2,500ms/key, gemini 7,500ms). `application.properties` (+`chunkai.llm.provider`, `groq.*` block). Python orchestrator: `config.py` (+`LLM_PROVIDER`, `GROQ_*`; drill-down embedding MUST match ingest provider), `llm.py` (ChatOpenAI→Groq for chat — correlation/report on `llama-3.3-70b-versatile`; `/embeddings` REST for query embedding; gemini path kept as fallback), `requirements.txt` (+`langchain-openai`). docker-compose orchestrator env += `LLM_PROVIDER`/`GROQ_API_KEY`. `.env.example`/`.env` += Groq block (`.env` gitignored, scaffolding commit `1ab1a14`).
- **Consistency rule (repeat of gateway javadoc):** vectors from different providers never mix — sessions embedded under Gemini must be re-ingested under Groq. GeminiClient/GeminiConfig KEPT as fallback provider (`LLM_PROVIDER=gemini`).
- Verified: `compileJava` clean; `py_compile` clean. NOT yet exercised against live Groq (owner must set real `GROQ_API_KEY` in `.env`). ⚠️ Groq embeddings endpoint rate limits are account-visible only — check console before sizing lanes.

---

_Phase 3 (superseded by the above; kept for detail):_ **2026-07-15 (Phase 3 DONE — LLM enrichment + embedding lanes over Kafka).** Branch `phase-3-enrich-lanes` (off `phase-2-chunker-parsers`). **Committed LOCALLY ONLY — NOT pushed, no PR** (owner directive "dont push any change"). One commit `b8a1d82`: _Add Layer-2 LLM enrichment lanes over Kafka_.

New `com.deepdocai.enrich` package + `common/messages/EnrichRequest` (all **additive** — Phase 1 spent the spec's v1 deletion lists):
- `EnrichRequest` (record) `{workId, sessionId, kind (ENRICH_WINDOW|EMBED_BATCH), chunkIds, attempt, notBeforeEpochMs}` + `retry(notBefore)` helper.
- `KafkaTopicsConfig` — `NewTopic` beans: `llm.enrich.requests` & `llm.enrich.retry.60s` partitions = `ApiKeyManager.getSlotCount()` (=5 here); `llm.enrich.dlq`/`log.ingest.dlq`=1; **re-declares `log.ingest.requests` at 6** (KafkaAdmin raised it from the Phase-1 auto-created 1).
- `EnrichProducer` — `workItemCount(windows)` + `produce(...)`: one ENRICH_WINDOW per **anomalous** window + one EMBED_BATCH per `chunkai.embedding.batch-size` (80) chunks across **all** windows (when `chunkai.embedding.enabled`). Spreads items across partitions by `floorMod(workId.hashCode, slotCount)` so a retried item returns to the same key-lane. **Wired into `IngestConsumer`**, which now sets `total_windows = workItemCount` (NOT raw window count — that's what the completion counter counts down) BEFORE flipping ENRICHING, then produces; if 0 work items it flips to CORRELATING itself.
- `EnrichConsumer` — `@KafkaListener(llm.enrich.requests, groupId=llm-workers, concurrency=#{@apiKeyManager.getSlotCount()})`, manual ack. Per record: `apiKey = getApiKey(partition)`, pace via `apiKeyManager.getSlot(partition)` `enforceRateLimit()`/`markCallMade()` (reuses `ApiKeySlot`, one owner thread per partition → lock-free 7.5s). ENRICH_WINDOW→`GeminiClient.generateContent` (JSON) → `FindingsWriter` upsert; EMBED_BATCH→`batchGenerateEmbeddings` → `SessionChunkRepository.updateEmbeddings`. 429→re-produce to `llm.enrich.retry.60s` (attempt+1); attempt≥3 **or any non-retryable error**→`llm.enrich.dlq`. **Every terminal outcome (success or DLQ) → `incrementEnrichedWindows` + completion check** so one failed item can't wedge completion.
- `RetryConsumer` — dedicated single-thread listener on `llm.enrich.retry.60s`: `Thread.sleep` until `notBeforeEpochMs`, then re-produce to `llm.enrich.requests` (keeps worker threads free — they never sleep 60s).
- `EnrichCompletion` — after each increment, if `enriched_windows >= total_windows` run `SessionRepository.flipEnrichingToCorrelating` (atomic `UPDATE ... WHERE analysis_status='ENRICHING'`); the one caller that updates 1 row triggers `OrchestratorClient.analyze` (POST `{orchestrator.url}/analyze/{id}`, best-effort ×3).
- `EnrichPrompts` — system+user builders; **required JSON** `{category,severity,title,explanation,confidence}`; `fingerprint = SHA-256(category + normalized title)` (title lower-cased, digits dropped, whitespace collapsed → recurring anomalies fold to one row). `FindingsWriter` — JDBC upsert `log_findings` ON CONFLICT (session_id,fingerprint): `occurrence_count++`, evidence `ARRAY(SELECT DISTINCT unnest(... || EXCLUDED...))`, time range LEAST/GREATEST. Evidence ids attached by **code**, never the model.
- Repo additions: `SessionChunkRepository.readChunks` (content+bucket, input order) + `updateEmbeddings` (batch `SET embedding=?::vector`); `MetricsWriter.metricContext` (compact metric lines for the window's bucket range → prompt); `SessionRepository.flipEnrichingToCorrelating`. `KafkaConsumerConfig.dlqTopicFor` maps the enrich topics → `llm.enrich.dlq`.

**Verified — booted `:8080` against live docker stack (⚠️ no `GEMINI_API_KEYS`, so real LLM output can't be produced; verified the full Kafka/DB wiring instead):** (a) all 5 topics provisioned with correct partitions — `log.ingest.requests`=**6**, `llm.enrich.requests`/`retry.60s`=**5**, both dlq=**1**; (b) consumer containers = **5 `llm-workers` + 1 `llm-retry`**; (c) upload the 28-line sample → `EnrichProducer` "Enqueued … **2 ENRICH_WINDOW, 1 EMBED_BATCH**", `total_windows=3`; all 3 consumed on **distinct partitions (0 & 2 → different key-lanes)**, each hit Gemini `400` (placeholder keys) → non-retryable → **`llm.enrich.dlq` end offset = 3**; (d) `EnrichCompletion` "**complete (3/3) → CORRELATING**" fired **exactly once**, session row `analysis_status=CORRELATING, total_windows=3, enriched_windows=3`; (e) `OrchestratorClient` POST `/analyze/{id}` attempted ×3 then gave up gracefully (orchestrator not up); (f) **retry lane** — injected a synthetic retry msg (`notBefore=now+12s`) → `RetryConsumer` held ~12s then "**Re-released work …**", `EnrichConsumer` re-consumed it (worker threads never stalled); (g) **findings dedup** — ran `FindingsWriter`'s exact upsert twice on one `(session_id,fingerprint)` → **1 row**, `occurrence_count=2`, evidence merged+deduped to 2 ids, time range widened to min/max.

**Notes for Phase 4+:** (a) **No `GEMINI_API_KEYS`** → real findings/embeddings and real 429s can't be produced locally; the 429 code path is verified via synthetic retry injection, the findings-upsert via direct SQL. Set 5 real keys to exercise the LLM path end-to-end. (b) On completion the session sits at **CORRELATING** and POSTs `{chunkai.orchestrator.url}/analyze/{id}` (default `http://localhost:8000`) — Phase 4 stands up that orchestrator; until then the POST just logs and gives up (session is durably CORRELATING and re-triggerable). (c) `EnrichConsumer` catches its own errors (429→retry, else→DLQ) so the shared `DefaultErrorHandler` rarely fires for it, but `dlqTopicFor` now routes it to `llm.enrich.dlq` as a fallback.

---

_Phase 2 (superseded by the above; kept for detail):_ **2026-07-15 (Phase 2 DONE — chunker + Layer-1 parsers + anomaly flag).** Branch `phase-2-chunker-parsers` (off `phase-1-sessions-minio`), local-only, commit `77874c8` + HANDOVER `b7a21b0`. New `com.deepdocai.ingest`: `IngestConsumer` (manual-ack `log.ingest.requests` listener CHUNKING→PARSING→ENRICHING, idempotent), `TimeWindowChunker` (60s buckets, ISO/space/syslog regexes, forward-fill, 500-line fallback), `LogWindowParser` iface + 7 parsers (API/DATABASE/ERRORS/LIFECYCLE/PERFORMANCE/AUTH/TRAFFIC; ErrorParser SHA-256 stack fingerprint; **no DEPENDENCY parser** per spec), `AnomalyDetector` (parser-flag / ≥5 WARN / latency>3×p95), `MetricsWriter` (additive `log_metrics` upsert), `storage/SessionChunkRepository` (JDBC batch insert). Shared `KafkaConsumerConfig` (`StringJsonMessageConverter`, manual ack, `DefaultErrorHandler`→`.dlq`) + `KafkaFailureMarker`; repo TX/status methods. DoD verified on booted app: 28-line log → 5 windows (2 anomalous), 44 `log_metrics` rows, document COMPLETED + staged file deleted, session ENRICHING.

---

_Phase 1 (superseded by the above; kept for detail):_ **2026-07-15 (Phase 1 DONE — sessions, MinIO staging, upload → Kafka).** Branch `phase-1-sessions-minio` (off `phase-0-infra`). **Committed LOCALLY ONLY — NOT pushed, no PR** (owner directive mid-session: "dont push any change"). Phase 0 (`phase-0-infra`, `e71e830`) remains done; see session log.

Two commits on this branch:
- `a73b3dc` **Remove v1 doc-QA pipeline** (46 files). The whole v1 doc-QA stack is coupled through `Chat` + the old `Document` shape, so it had to be deleted *before* `Document` could be reshaped to the v2 `documents` table — this **front-loaded the spec's Phase 2 & 3 deletion lists into Phase 1** (those phases now have little/nothing left to delete). Deleted: doc processors + factory, `ChunkingService`, `DocumentProcessingService`, `RagService`, `EmbeddingService`/`BatchEmbeddingService`, `RecursiveSummarizationService`, `KeyedWorkerPool`/`WorkItem`, prompt templates, entities `Chat`/`Document`/`DocumentChunk`/`ProcessingJob`/`QueryHistory` (+ repos), `Chat`/`Document`/`Query` controllers + DTOs, old `FileStorageService`/`LocalFileStorageService`, `SchedulingConfig`, `FileTypes`/`FileUtils`/`TokenCounter`, orphan `llm.keymanager.ApiKeyManager`. **Kept & reused** (spec §5): `GeminiClient` (+ response models), `ApiKeyManager` (`llm.key`), Gemini/WebClient config, JWT/security stack, `GlobalExceptionHandler`, `User`/`UserRepository`, `ProcessingStatus` (its PENDING/PROCESSING/COMPLETED/FAILED values match v2 `documents.processing_status`). Trimmed `User` (dropped `documents` collection) and `UserController` (profile-only; its stats used deleted repos).
- **Phase 1 additions** (HEAD of branch): `Session` entity + `SessionRepository` (`@Modifying` `setStatus`/`incrementEnrichedWindows`/`setTotalWindows`); rewritten `Document` entity (v2 shape, **id assigned by caller** — no `@GeneratedValue`, so the MinIO key/`fileUrl` can be built before insert) + `DocumentRepository` (dup-check `findBySessionIdAndOriginalFileNameAndFileSizeBytes`); new **`com.deepdocai.storage`** package — `FileStorageService` interface, `MinioStorageService` (bucket auto-create in `@PostConstruct`), `SessionChunkTableManager` (UUID-regex-validated DDL for `log_chunks_s_<tid>` + HNSW/GIN/time indexes — injection guard), `StorageException`; `common/messages/IngestRequest` (record); `common/constants/AnalysisStatus` + `KafkaTopics`; `SessionController` (`/api/v1/sessions` CRUD — create provisions chunk table & rolls back the row if DDL fails; delete removes row then drops table); reworked `DocumentController` (`POST /api/v1/sessions/{sessionId}/documents`: validate → MinIO store (outside TX) → insert row (short TX) → produce `IngestRequest` after commit → 202; duplicate → 200 with existing; lost race on unique key → delete staged object + return winner). Kafka producer props (String key, `JsonSerializer` value, type-headers off, idempotent, acks=all) added to `application.properties`. Error handling uses explicit `ResponseEntity` statuses (the KEEP `GlobalExceptionHandler` has a catch-all `Exception`→500 that would swallow `ResponseStatusException`).

**Verified (DoD) — app booted `:8080` against the live docker stack:** register+JWT → `POST /sessions` creates the row and provisions `log_chunks_s_<tid>` (psql confirms `vector(768)` col + HNSW index) → upload `sample.log` → **116 B object in MinIO** at `staging/{sessionId}/{documentId}` (`mc stat`) + **`documents` row** PENDING with `file_url=s3://staging/...` (psql) + **message on `log.ingest.requests`** key=sessionId, JSON `IngestRequest` no type headers (kafka-console-consumer) → `DELETE /sessions/{id}` drops the chunk table (`to_regclass`→NULL) and cascades. Boot log clean (only benign dialect/open-in-view/CORS warns + the expected first-produce `UNKNOWN_TOPIC_OR_PARTITION` auto-create warning). Build/run: `JAVA_HOME=jdk-21` (Gradle 8.5), Postgres `:5434`, Rancher Desktop. **Known follow-up (not in Phase 1 DoD):** staged MinIO objects are NOT removed on session-delete (orphaned); staged-file cleanup belongs to Phase 2's `staged_file_deleted` path.

## Next steps (in order)

1. ~~Commit the current working tree as a clean baseline.~~ **DONE** (`7db18de` on `main`).
2. **Execute [03-implementation-spec.md](03-implementation-spec.md) Phases 0→5, in order** — each phase is one PR-sized, self-contained task with its own definition-of-done and deletion list. **Phases 0–5 ALL DONE and verified (backend + Python orchestrator complete).** **NEXT: v2 frontend rework** (owner-approved) — rework `examprep-frontend/` off the v1 exam-prep UI: reuse Login/Register/AuthContext + the auth API; replace the chats/documents/query screens with **sessions list → upload → live SSE progress → metrics/findings/incidents/report tabs → drilldown chat**; rewrite `src/services/api.js` against the Phase-1/Phase-5 endpoints. Backend CORS already allows `http://localhost:5173`. Test with `npm run dev`. (Out of scope until then: tests, Flyway, lag-aware partitioner.)
   - ⚠️ **Owner directive (mid Phase 1): "dont push any change".** All work is **local commits only** — do NOT `git push` or open PRs without explicit approval. (`phase-0-infra` was pushed *before* this directive; every branch since is local-only.)
   - **Run-it-locally cheatsheet:** infra `docker compose up -d` (postgres :5434, kafka :9092, minio :9000/:9001); backend `JAVA_HOME=jdk-21` + load `.env` into the process env, `./gradlew :deepdocai-backend:bootRun` (:8080); orchestrator `rag-orchestrator/.venv/Scripts/python -m uvicorn app.main:app --port 8000` with `.env` loaded. `.env` needs `GROQ_API_KEY` (chat) + `GEMINI_API_KEY` (embeddings) + `EMBEDDING_PROVIDER=gemini`. `.env` is NOT auto-loaded by Gradle/uvicorn — export it into the process first.
3. **Owner decision recorded 2026-07-15: v1 doc-QA mode is DELETED during implementation** (not kept as secondary mode — overrides earlier statements in 02/README). Git history is the archive.
4. Skip tests during implementation (owner: separate pass, cheaper model). Frontend after backend completes.
5. Cleanup backlog: Flyway, frontend rename, real commit messages, lag-aware partitioner, golden-corpus regression set.

## Session log

| Date | Session summary |
|---|---|
| 2026-07-14 | Explored codebase; wrote docs 01/02 + handover. Cleaned root md files; README rewritten twice (HLD, then senior tone). Long design exploration: pgvector top-k/filtered-ANN, table-per-session (accepted with corpus-scale defense), positioning pivot to log intelligence, two-layer extraction + materialized findings. 02 rewritten as FINAL; README repositioned; decisions locked above. |
| 2026-07-15 | Clarity pass on 02: §6 data model expanded (ER diagram, plain-word per-table definitions, evidence-chain mental model). §3 rebuilt as THE single interview whiteboard diagram — numbered arrows 1–12 (analysis) + Q1–Q3 (query), single top-down spine, no subgraphs (subgraph layouts scrambled in the owner's renderer). §10 is now a step-by-step narrative table keyed to those same numbers ("say these sentences while drawing the arrows"). Keep this structure — owner wants diagrams reproducible on a whiteboard. No code changes. |
| 2026-07-15 (later) | Data model amendments from owner (users/documents/sessions attributes, staging blob lifecycle → locked decisions #8/#9); MinIO chosen as staging store for local AND prod. Implementation prep: wrote **schema-v2.sql** (real DDL, fresh-DB, per-session table template) and **docs/03-implementation-spec.md** (agent-executable Phases 0–5 with binding global decisions, message schemas, per-phase deletions + definition-of-done). Owner decided: delete v1 doc-QA mode entirely; no tests during implementation. Still no code changes to src/. |
| 2026-07-15 (Phase 0) | Committed baseline on `main` (`7db18de`). Implemented **Phase 0** on branch `phase-0-infra`: `docker-compose.yml` swap init.sql→schema-v2.sql + add `apache/kafka` (KRaft) & `minio` services with healthchecks (+ named volumes); `build.gradle.kts` adds `spring-kafka` + `io.minio:minio:8.5.17`; `application.properties` gains the `chunkai.*` block and `ddl-auto=none`. Verified all 3 containers healthy, fresh schema-v2 DB (7 tables + extensions), Kafka reachable, app boots & serves (`/v3/api-docs` 200). Infra/config only — no `src/` changes. PR opened → `main`. |
| 2026-07-15 (Phase 1) | Branch `phase-1-sessions-minio`, **local commits only** (owner said "dont push any change"). Commit `a73b3dc` deletes the entire coupled v1 doc-QA stack (46 files) — forced early because reshaping `Document` + replacing `Chat` breaks the whole chain; front-loads the Phase 2/3 deletion lists. Additions commit builds the v2 ingest front-door: `Session`/`SessionRepository`, v2 `Document`/`DocumentRepository`, `com.deepdocai.storage` (`MinioStorageService`, `SessionChunkTableManager`, `FileStorageService`), `IngestRequest` record, `AnalysisStatus`/`KafkaTopics`, `SessionController` + reworked `DocumentController`, Kafka JSON-producer props. DoD verified end-to-end on a booted app: session create → chunk table w/ HNSW; upload → 116 B in MinIO + `documents` row + JSON message on `log.ingest.requests`; session delete → chunk table dropped + cascade. Trimmed `User`/`UserController` to compile without doc-QA. |
| 2026-07-15 (Phase 2) | Branch `phase-2-chunker-parsers` (off Phase 1), **local commit only**. Built the `com.deepdocai.ingest` Layer-1 pipeline: `IngestConsumer` (manual-ack `log.ingest.requests` listener, CHUNKING→PARSING→ENRICHING, idempotent), `TimeWindowChunker` (60s buckets + syslog/ISO/space regexes + 500-line fallback), `LogWindowParser` + 7 regex parsers (API/DATABASE/ERRORS/LIFECYCLE/PERFORMANCE/AUTH/TRAFFIC; ErrorParser SHA-256 stack fingerprint), `AnomalyDetector` (error/exception/OOM/deadlock/health, ≥5 WARN, latency>3×p95), `MetricsWriter` (additive `log_metrics` upsert), `storage/SessionChunkRepository` (JDBC batch insert into `log_chunks_s_<tid>`). Added shared Kafka consumer config (`StringJsonMessageConverter`, manual ack, `DefaultErrorHandler`→`.dlq` + `KafkaFailureMarker`) and repo TX/status methods. DoD verified on booted app: 28-line synthetic log → 5 windows (2 anomalous), 44 `log_metrics` rows with exact counts + avg/p95, document COMPLETED + staged file deleted, session at ENRICHING. No LLM keys in env (noted for Phase 3–4). |
| 2026-07-15 (Phase 3) | Branch `phase-3-enrich-lanes` (off Phase 2), **local commit `b8a1d82` only**. Built the `com.deepdocai.enrich` Layer-2 LLM lanes + `common/messages/EnrichRequest`: `KafkaTopicsConfig` (enrich topics partitioned = key-slot count; `log.ingest.requests` raised to 6), `EnrichProducer` (ENRICH_WINDOW per anomalous window + EMBED_BATCH per 80 chunks; wired into `IngestConsumer`, `total_windows`=work-item count), `EnrichConsumer` (concurrency=slot count, per-partition `ApiKeySlot` 7.5s pacing, ENRICH_WINDOW→`FindingsWriter` upsert / EMBED_BATCH→`updateEmbeddings`, 429→retry.60s, attempt≥3/error→dlq, every terminal outcome increments), `RetryConsumer` (single thread, sleeps to `notBefore`), `EnrichCompletion` (atomic ENRICHING→CORRELATING flip → `OrchestratorClient` POST `/analyze/{id}`), `EnrichPrompts` (JSON out; SHA-256 fingerprint), `FindingsWriter` (ON CONFLICT dedup). No `GEMINI_API_KEYS` → verified wiring not LLM output: 5 topics w/ correct partitions, 5 llm-workers+1 retry, upload → 2 ENRICH_WINDOW+1 EMBED_BATCH across 2 key-lanes → 3 to dlq (bad keys) → completion flip (3/3) exactly once → orchestrator POST attempted; retry lane held ~12s then re-released a synthetic msg; findings dedup upsert (occurrence++/evidence-merge/time min-max) validated via direct SQL. |
| 2026-07-15 (Phase 4 — verify + fix) | Branch `phase-4-orchestrator`, **local only** (`8b2950b` + fixes folded into owner's `4a1535a`). Provider pivot to **Groq chat + Gemini embeddings** reconciled with owner's commits (`202a8f2`/`0279276`/`3346632`). Ran the orchestrator (both LangGraph graphs) against a live session: fixed **Graph 1 "recursion limit 25 reached"** (dynamic `recursion_limit = 12 + n_findings*(MAX_CORRELATE_ATTEMPTS*2+1)` — llama's ground_check rejects grounding more than Gemini, inflating the bounded regenerate loop) and completed the **drilldown `best_docs` fallback** (`generate_node` graded→docs→best_docs so an over-strict grader/degrading rewrite can't answer from nothing) + `db.count_findings`. Verified **full E2E DONE**: 16 findings → 6 incidents → ~1.4 KB report, **5/5 chunks embedded** (Gemini @768, previously NULL), drilldown returns cited answers. |
| 2026-07-15 (Phase 5) | Branch `phase-5-query-sse` (off Phase 4), **local commit only**. New `com.deepdocai.analysis`: `AnalysisQueryController` (GET metrics/findings/incidents/report/evidence + POST drilldown proxy), `AnalysisReadRepository` (JDBC reads; evidence via validated-UUID chunk-table name), `AnalysisProgressController` (SseEmitter + single `@Scheduled(1500)` sweep → `{status,enriched,total}`, completes on DONE/FAILED), `SessionAccess` guard (returned-not-thrown → 404 missing / 403 not-owner, because the KEEP `GlobalExceptionHandler` catch-all would remap a thrown status to 500). Converted `OrchestratorClient` **RestClient→WebClient** (hand-built RestClient sent an empty body → FastAPI 422). Added `@EnableScheduling`; `SecurityConfig` permits `DispatcherType.ASYNC/ERROR` (kills SSE-completion `AccessDenied` noise). **DoD via curl on a fresh user + real pipeline run:** create→upload→**SSE ENRICHING 1/3→2/3→CORRELATING→REPORTING→DONE**→metrics(44)/findings(16)/incidents(6)/report(1404 ch)/evidence(chunk-table)→drilldown cited answer; gates 403/404/403. CORS already allows `:5173`. Backend builds (JDK 21) + boots clean. NEXT: v2 frontend rework. |
| 2026-07-15 (frontend v2 shell resumed) | Fixed the React frontend API client (`src/services/api.js`) so it now sends the bearer token correctly and streams SSE progress against the phase-5 backend. Verified `npm run build` succeeds. Current continuation point is the v2 shell QA/polish pass in `examprep-frontend/`. |
